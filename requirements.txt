# Local development & testing dependencies.
# These let you run and test the binmoments package on your machine with plain
# PySpark — no Databricks cluster needed for the unit/logic tests.
#
# NOTE on databricks-connect: do NOT pin it here. When you wire up Databricks
# execution later, install a databricks-connect version that MATCHES your
# pyspark install in the same environment causes conflicts.

numpy>=1.26
pandas>=2.0
scipy>=1.11
pyyaml>=6.0
pytest>=8.0
ruff>=0.5
