# Local development & testing dependencies.
# The binmoments package is pure Python (numpy), so you can run and test it on
# your machine with no Spark and no Databricks cluster — `pip install -e .` then
# `pytest`. Spark lives only in the notebooks; the package never imports it.
#
# NOTE: do NOT pin pyspark or databricks-connect here. Databricks serverless
# already provides PySpark; a local copy that does not match the runtime causes
# conflicts. The package's storage-agnostic logic doesn't need either.

numpy>=1.26
pyyaml>=6.0
pytest>=8.0
ruff>=0.5