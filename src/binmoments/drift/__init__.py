"""Drift detection: Wasserstein distance, baseline policy, cross-schema comparison.

See ADR-005 (intra-instrument drift; Wasserstein; configurable per-instrument baselines;
self-calibrated thresholds) and ADR-016 (CDF resampling onto a fixed grid for
cross-schema-version comparison).
"""
